[phases.setup]
nixPkgs = ['php82', 'php82Extensions.mongodb', 'php82Packages.composer', 'nodejs_18']

[phases.install]
cmds = ['composer install --no-dev --optimize-autoloader --no-interaction', 'npm ci', 'npm run build']

[start]
cmd = 'php artisan serve --host=0.0.0.0 --port=$PORT'