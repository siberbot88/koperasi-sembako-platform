<div>
    @if($cartCount > 0)
    <span class="absolute -top-0.5 -right-0.5 w-4 h-4 bg-koperasi-primary border border-koperasi-black rounded-full text-[10px] font-bold flex items-center justify-center">{{ $cartCount }}</span>
    @endif
</div>
